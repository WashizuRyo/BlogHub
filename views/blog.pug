extends layout 

block content 
  div.card.my-3
    h4.card-header #{blog.blogTitle}
    div.card-body
      p.card-text(style="white-space: pre;") #{blog.blogText}
    div.card-footer 作成者: #{blog.user.username}
  - const isMine = parseInt(user.id) === blog.user.userId 
  if isMine 
    div 
      a.btn.btn-primary(href=`/blogs/${blog.blogId}/edit`) ブログを編集する 
  h3.my-3 コメント
  table.table.table-bordered
    tr
      th 作成者
      td コメント内容
    tr
      each comment in comments
          th #{comment.username}
          td
            if comment.userId === parseInt(user.id)
              p#self-comment #{comment.comment}
              button(
                data-blog-id=blog.blogId 
                data-user-id=user.id)#self-comment-button.btn.btn-info 編集
            else 
              p #{comment.comment}
